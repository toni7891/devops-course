apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "templating-deep-dive.fullname" . }}-test-connection"
  labels:
    {{- include "templating-deep-dive.selectorLabels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "templating-deep-dive.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
