# שאלות ראיון - Git & GitHub

1. מה זה Git ולמה משתמשים בו?
2. מה ההבדל בין Git ל-GitHub?
3. מה זה מאגר (repository) ב-Git?
4. מה ההבדל בין `git init` ל-`git clone`?
5. מהם שלושת השלבים (האזורים) ב-Git?
6. מה ההבדל בין `git add` ל-`git commit`?
7. מה ההבדל בין `git pull` ל-`git fetch`?
8. מה ההבדל בין `git merge` ל-`git rebase`?
9. מה זה קונפליקט מיזוג (merge conflict) ואיך פותרים אותו?
10. מה זה ענף (branch) ב-Git ולמה משתמשים בענפים?
11. איך יוצרים, עוברים ומוחקים ענפים ב-Git?
12. מה זה `HEAD` ב-Git?
13. מה זה מצב HEAD מנותק (detached HEAD)?
14. מה ההבדל בין `git reset`, `git revert` ו-`git checkout`?
15. מהם שלושת המצבים של `git reset` (soft, mixed, hard)?
16. מה זה `git stash` ומתי משתמשים בו?
17. מה זה קובץ `.gitignore` ולמה הוא חשוב?
18. מה ההבדל בין מיזוג fast-forward למיזוג three-way?
19. מה זה `git cherry-pick` ומתי הוא שימושי?
20. מה זה `git bisect` ואיך הוא עוזר לאתר באגים?
21. מה ההבדל בין `git diff`, `git diff --staged` ו-`git diff HEAD`?
22. מה הם תגיות (tags) ב-Git ומה ההבדל בין תגית קלה (lightweight) לתגית מוערת (annotated)?
23. מה זה בקשת משיכה (Pull Request) ואיך היא משתלבת בעבודת צוות?
24. מה זה פורק (fork) ב-GitHub ומה ההבדל בינו לבין ענף (branch)?
25. מה זה GitHub Actions ואיך משתמשים בו ל-CI/CD?
26. מה זה Git hook? ציינו כמה hooks נפוצים ואת השימושים שלהם.
27. איך עושים squash לקומיטים ולמה עושים את זה?
28. מה זה `git reflog` ואיך הוא יכול לעזור לשחזר קומיטים שאבדו?
29. מה ההבדל בין `git rm` לבין מחיקת קובץ רגילה?
30. מה זה Git submodule ומתי משתמשים בו?
