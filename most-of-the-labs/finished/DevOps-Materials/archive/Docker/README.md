# Docker

Introduction to Docker and containerization for DevOps.

## Labs

- **docker_lab_basics** — First steps with Docker: pulling images, running containers, inspecting them, and understanding the container lifecycle.

## Getting Started

Each lab folder contains a `README.md` with instructions. Labs are also available as `.tar.gz` archives — see `STUDENT_COMMAND.txt` for download instructions.
