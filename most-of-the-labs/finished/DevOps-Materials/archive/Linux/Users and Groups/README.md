# Users and Groups

Labs covering Linux user and group management for system administration.

## Labs

| Lab | Description |
|-----|-------------|
| `linux_users_groups_management` | Creating users (`useradd`), setting passwords, creating groups (`groupadd`), and managing group membership |

Each lab folder contains a `README.md` with full instructions.
