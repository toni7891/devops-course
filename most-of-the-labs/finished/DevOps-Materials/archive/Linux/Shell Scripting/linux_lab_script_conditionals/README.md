# Linux Lab – Script Conditionals

Master bash conditional statements to build safer, smarter scripts. Learn to check file and directory existence, validate user input, and create robust pre-deployment validation scripts.

## Learning Objectives

After this lab you will be able to:
- Check if files exist with `[ -f ]`
- Check if directories exist with `[ -d ]`
- Validate empty input with `[ -z ]`
- Use `if/elif/else` for multiple conditions
- Use negation with `[ ! ]`
- Build comprehensive validation logic

## Getting Started

Open `TASKS.md` to begin the lab exercises.

## Duration

25-35 minutes

## Prerequisites

- Script Basics, Variables, and Input labs (or equivalent knowledge)
- Linux system with bash
- A text editor
