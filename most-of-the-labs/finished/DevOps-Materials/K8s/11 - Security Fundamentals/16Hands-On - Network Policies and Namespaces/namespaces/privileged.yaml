apiVersion: v1
kind: Namespace
metadata:
  name: privileged
  labels:
    pod-security.kubernetes.io/enforce: privileged # Enforce the privileged policy, which allows all capabilities and privileged containers.
    pod-security.kubernetes.io/warn: baseline # Warn about any violations of the baseline policy, which is a more restrictive set of security settings.
