apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: orders-app
spec:
  selector:
    app: backend
  ports:
    - port: 3000
      targetPort: 3000
  type: ClusterIP   # Ingress routes /api/* here; not exposed directly
