# Commands

```bash
# See where you are (current directory)
pwd

# Run script from current directory (relative path)
./deploy.sh
./check_path.sh
./where_am_i.sh

# Or with full path (absolute)
# /path/to/scripts/deploy.sh
```
