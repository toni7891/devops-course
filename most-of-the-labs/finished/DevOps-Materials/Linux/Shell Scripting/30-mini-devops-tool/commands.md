# Commands

```bash
chmod +x ops_tool.sh
./ops_tool.sh
```

Demo: check, deploy, status, backup, pipeline (simulated). For real CI/CD with scripts depending on each other (exit codes), see 28-exit-codes-practice: pipeline.sh, ci_validate.sh, ci_build.sh, ci_test.sh, ci_deploy.sh.
