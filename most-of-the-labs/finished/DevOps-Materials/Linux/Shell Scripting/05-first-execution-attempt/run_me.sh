#!/bin/bash

# Two ways to run:
# 1. ./run_me.sh  -> needs execute permission (chmod +x)
# 2. bash run_me.sh  -> interpreter gets the file, no permission needed

echo "Script is running!"
echo "If you see this, execution worked"
