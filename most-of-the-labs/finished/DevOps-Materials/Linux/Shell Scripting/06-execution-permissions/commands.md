# Commands

```bash
# See current permissions (no x = not executable)
ls -l deploy.sh

# Give execute permission
chmod +x deploy.sh
chmod +x check.sh
chmod +x backup.sh

# Now run with ./
./deploy.sh
./check.sh
./backup.sh
```
