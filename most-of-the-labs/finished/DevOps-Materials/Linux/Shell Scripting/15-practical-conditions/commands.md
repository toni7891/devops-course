# Commands

```bash
chmod +x deploy.sh check.sh backup.sh ensure_dirs.sh release_dir.sh
./deploy.sh
./check.sh
./backup.sh
./ensure_dirs.sh
./release_dir.sh
```

Demo: run twice - first creates dirs, second uses existing. ensure_dirs creates logs, backup, tmp. release_dir creates releases/YYYY-MM-DD.
