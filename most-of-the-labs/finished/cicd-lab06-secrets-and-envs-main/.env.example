# App status: DEV or TEST
APP_ENV=DEV
# Port for the HTTP service
PORT=3000

# MongoDB connection — URI built dynamically from these
MONGO_USER=your_db_user
MONGO_PASS=your_db_password
MONGO_HOST=cluster0.some-domain.mongodb.net
MONGO_DB=express-app