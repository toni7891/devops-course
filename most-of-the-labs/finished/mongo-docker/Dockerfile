# Part 6 — Custom MongoDB image
#
# TODO: base off the official mongo image (pin a tag, not :latest)
# FROM mongo:<tag>

# Part 7 — Auto-seed on first run
# TODO: copy your init scripts into the image so MongoDB runs them on a
#       fresh data directory:
# COPY docker-entrypoint-initdb.d/ /docker-entrypoint-initdb.d/

# TODO (optional): EXPOSE the MongoDB port for documentation
# EXPOSE 27017

# TODO (optional, advanced): add a custom mongod.conf and CMD to use it
