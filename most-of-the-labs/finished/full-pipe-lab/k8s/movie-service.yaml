apiVersion: v1
kind: Service
metadata:
  name: movie-api
  namespace: movies
  labels:
    app: movie-api
    team: dev
    environment: prod
spec:
  type: NodePort
  selector:
    app: movie-api
  ports:
  - port: 3000
    targetPort: 3000
